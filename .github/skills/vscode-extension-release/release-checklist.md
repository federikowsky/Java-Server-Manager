# VS Code Extension Release Checklist

1. Extension entrypoint resolves correctly.
2. Contributed commands match actual handlers.
3. Tree view context values still match manifest menus.
4. Build scripts still package the extension successfully.
5. Tests are either working or explicitly called out as a release blocker.
6. README and changelog do not overclaim maturity.