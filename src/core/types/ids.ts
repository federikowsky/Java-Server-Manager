/** UUID v4 string identifying a server. */
export type ServerId = string;

/** UUID v4 string identifying a deployment. */
export type DeploymentId = string;

/** UUID v4 string identifying a template. */
export type TemplateId = string;

/** UUID v4 string identifying an operation. */
export type OperationId = string;
