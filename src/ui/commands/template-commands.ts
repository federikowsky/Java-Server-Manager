// Template commands were removed as part of the SPA dashboard migration.
// This module remains to avoid import breakage, but it no longer registers any commands.

export function registerTemplateCommands(): any[] {
  return [];
}
